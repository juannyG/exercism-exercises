module Acronym (abbreviate) where
--import qualified Data.Text as T
--import           Data.Text (Text)

abbreviate :: String -> String
abbreviate xs =
  error "nope"

--letters :: String
--letters = ['a'..'z'] ++ ['A'..'Z']
--abbreviate :: Text -> Text
--abbreviate xs =
--  let ws = T.words(T.toTitle xs)
--  in T.pack([T.head(w) | w <- ws, T.head(w) `elem` letters])
